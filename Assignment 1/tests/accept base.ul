void foo(int x, float abc)
{
    char a;
    boolean y;

    if (x == true)
    {
        print abc;
    }
    println abc;
    return abc;
}

string[3] test()
{
    float z;

    foo(1, 2.5, true, 'z', "words", false);
    if (false)
    {
        if (z < 3)
        {
            println z;
        } else
        {
            asdf = x * a[some_var + some_other];
        }
        return asdf;
    }
    else
    {
        print abc;
    }
}

char bar(boolean b)
{
    int x;

    ;
    x;
    x = 5;
    y = 3.14;
    z = 'c';
    println (1 - 1);
    abc = (5);
    test = some_array[12];
    str = "this is a string";
    a[1] = 50;
    while (true)
    {
        print true;
    }
}
