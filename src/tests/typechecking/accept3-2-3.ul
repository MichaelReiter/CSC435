// For an invocation of function f, the number and types of arguments in the
// invocation must match the number and types of parameters in the function declaration

int foo(int x, int y, int z) {
    return x + y + z;
}

void main() {
    int i;
    i = 0;

    i = foo(i, i, i);
}
